
package misssclase;

/**
 *
 *  @author Pablo Aaron ALmaraz Avalos 18550326
 */
public class mejorbanco {
    //atributos de la clase
    //siempre tienen que ser privados
    private String nombre;
    private double saldo;
    //siempre debe existir un intercambio de los datos
    //intermedirarios son metodos: get(lectura) y set(escritura)
  //metodos 
    //nivel de acceso - valor de retorno - nombre(variables de entrada)
    //leer datos de nuestra clase
    public String getNombre()
            {
        return nombre;
            
            }
    public double getSaldo()
            {
            return saldo;
            }
    //escribir datos en nuestra clase
    public void setNombre(String sVal)
    {
    nombre = sVal;
    
    }
    public void setSaldo(double dVal)
    {
    saldo = dVal;
    }
    
}
